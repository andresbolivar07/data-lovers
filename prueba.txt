const printStatistics = (director) => {

    if (document.getElementById('container')){
        const cleanContainer = document.getElementById('container');
        document.getElementById('root').removeChild(cleanContainer);
    }
    let newDiv = document.createElement('div');
    newDiv.setAttribute('id', 'container');

    document.getElementById('root').appendChild(newDiv);

    director.forEach(element => {
        const template = `
        <div class="card">
            <ul>
                <li class="title">${element.name}
                </li>
            </ul>
        </div> `;
            
            newDiv.innerHTML += template;
    });
}

const statisticsBtn = document.getElementById("Statistics");
statisticsBtn.addEventListener('click', () => {
    const filterStatistic = computeStats(data);
    printStatistics(filterStatistic);
});